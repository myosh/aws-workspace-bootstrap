# 🧰 myosh AWS Workspace Bootstrap

This public repo provides a safe and simple way to bootstrap a developer's workspace inside an AWS-hosted environment.

---

## 🚀 Quick Start (for Developers)

1. **Login to your new workspace (via SSH or AWS Workspaces Client)**

2. **Run this command:**

```bash
bash <(curl -s https://raw.githubusercontent.com/myosh/aws-workspace-bootstrap/develop/bootstrap.sh)
